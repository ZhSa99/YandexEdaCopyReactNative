import { scale } from "react-native-size-matters"

export const bottomTabElemSizes = {
  tabBarLabelFontSize: 10.5,
  bottomNavigatorHeight: 70
}

export const drawerElemSizes = {
	headerLabelFontSize: {
		first: scale(13),
		second: scale(15.5),
	},
	yandexBonusFontSize: scale(18),
}
